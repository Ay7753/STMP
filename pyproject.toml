[project]
name = "stmp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
   "python-dotenv>=1.2.1",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["api", "email"]
